
// Contains the colors used in the application

import 'package:flutter/material.dart';

class AppColors{
  static const Color primaryColor =  Color(0xFFCC287A);
  static const Color textColor = Color(0xFF191970);
}